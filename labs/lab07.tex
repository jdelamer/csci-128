\documentclass{cslab}

\usepackage{listings}
\usepackage{xcolor}

% Python code styling
\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    stringstyle=\color{red},
    commentstyle=\color{green!50!black}\itshape,
    columns=fullflexible,
    showstringspaces=false,
    breakatwhitespace=false,
    breaklines=true,
    frame=single,
    tabsize=4
}

% Lab information
\labnumber{07}
\labtitle{Tabular Data and CSV Files}
\course{CSCI 128: Coding for Problem Solving}
\courseshort{CSCI 128}
\semester{Winter 2026}
\timelimit{2 hours}
\logoimage{LogoWordsBottom.png}

\begin{document}

\maketitle

\begin{objectives}
    \item Understand the structure of CSV files (rows, columns, headers)
    \item Read CSV files using both \texttt{csv.reader} and \texttt{csv.DictReader}
    \item Access and convert column values from strings to numbers
    \item Implement statistics functions (min, max, average) from scratch
    \item Filter rows based on column values using custom functions
    \item Write results to new CSV files using \texttt{csv.DictWriter}
    \item Build a complete data pipeline that loads, processes, and saves tabular data
\end{objectives}

\begin{grading}
\textbf{Participation-Based Grading:} Complete the tasks during the lab session and get checked off by the instructor or TA.

\textbf{To receive credit:}
\begin{itemize}
    \item Attend the entire lab session
    \item Complete Tasks 1--6 (Bonus is optional)
    \item Submit your \texttt{.py} files and generated CSV files on Moodle
    \item Get checked off by the instructor or TA before leaving
\end{itemize}
\end{grading}

\section{Introduction}

Just like images are 2D grids of pixels, spreadsheets are 2D grids of values-rows represent individual records and columns represent attributes. CSV (Comma-Separated Values) files are the simplest and most universal format for storing this kind of tabular data, and Python's built-in \texttt{csv} module makes reading and writing them straightforward. By the end of this lab you will be able to load a dataset, compute statistics, filter records, and save results, the core loop of real-world data analysis.

\section{Setup}

\begin{setup}
\textbf{Before you begin:}
\begin{enumerate}
    \item Create a folder: \texttt{csci128/lab07}
    \item \textbf{No extra installs needed!} The \texttt{csv} module ships with every Python installation, you never need to run \texttt{pip install csv}. Just \texttt{import csv} and you are ready to go.
    \item Inside \texttt{lab07}, create a subfolder called \texttt{data}
    \item Create a new file \texttt{data/students.csv} and paste in the following content exactly:
\end{enumerate}

\begin{lstlisting}[language={},numbers=none,frame=single]
name,grade,score,city
Alice,A,92,Halifax
Bob,B,78,Truro
Carol,A,95,Halifax
David,C,65,Sydney
Emma,B,81,Truro
Frank,A,88,Halifax
Grace,C,62,Sydney
Henry,B,75,Truro
\end{lstlisting}

Save the file and confirm it appears at \texttt{lab07/data/students.csv} before continuing.
\end{setup}

\section{Lab Tasks}

\task{Exploring a CSV File}

Before using any special tools, let's see what a CSV file actually looks like when read as plain text, then learn the clean Python way.

\textbf{Create \texttt{explore\_csv.py}:}

\begin{lstlisting}
import csv

# -------------------------------------------------------
# The manual way: open like a text file and split on ','
# -------------------------------------------------------
print("=== Manual reading ===")
with open("data/students.csv", "r") as f:
    for line in f:
        line = line.strip()          # remove trailing newline
        fields = line.split(",")
        print(fields)

# -------------------------------------------------------
# The clean way: use csv.reader
# -------------------------------------------------------
print("\n=== csv.reader ===")
with open("data/students.csv", newline="") as f:
    reader = csv.reader(f)
    for row in reader:
        print(row)

# -------------------------------------------------------
# Print just the header, then count data rows
# -------------------------------------------------------
print("\n=== Header and row count ===")
with open("data/students.csv", newline="") as f:
    reader = csv.reader(f)
    header = next(reader)           # read (and consume) the first row
    print("Columns:", header)
    count = 0
    for row in reader:
        count += 1
    print("Number of data rows:", count)

# -------------------------------------------------------
# Experiment 1: print only the name column (index 0)
# -------------------------------------------------------
print("\n=== Names only ===")
with open("data/students.csv", newline="") as f:
    reader = csv.reader(f)
    next(reader)                    # skip header
    for row in reader:
        print(row[0])

# -------------------------------------------------------
# Experiment 2: print only rows where grade == "A"
# -------------------------------------------------------
print("\n=== Grade A students ===")
with open("data/students.csv", newline="") as f:
    reader = csv.reader(f)
    next(reader)
    for row in reader:
        if row[1] == "A":
            print(row)

# -------------------------------------------------------
# Experiment 3: count students from Halifax
# -------------------------------------------------------
print("\n=== Halifax count ===")
halifax_count = 0
with open("data/students.csv", newline="") as f:
    reader = csv.reader(f)
    next(reader)
    for row in reader:
        if row[3] == "Halifax":
            halifax_count += 1
print("Students from Halifax:", halifax_count)
\end{lstlisting}

\begin{note}
The first row of a CSV file is called the \textbf{header row}-it gives each column a name. Calling \texttt{next(reader)} reads that one row and advances the reader past it, so every subsequent iteration gives you a data row only. If you forget to skip the header you will accidentally try to process the column names as data!
\end{note}

\textbf{Run the script and check:}
\begin{itemize}
    \item The manual split and the \texttt{csv.reader} output look the same-good!
    \item The header is \texttt{['name', 'grade', 'score', 'city']}
    \item There are exactly 8 data rows
    \item Three students are from Halifax (Alice, Carol, Frank)
\end{itemize}

% -----------------------------------------------------------
\task{Accessing Columns with DictReader}

\texttt{csv.reader} gives you lists-you have to remember that index 2 is the score. \texttt{csv.DictReader} is smarter: it reads the header automatically and gives you each row as a dictionary, so you can write \texttt{row['score']} instead of \texttt{row[2]}.

\textbf{Create \texttt{dict\_reader.py}:}

\begin{lstlisting}
import csv

# -------------------------------------------------------
# Load with DictReader and print name + score
# -------------------------------------------------------
print("=== All students (name, score) ===")
with open("data/students.csv", newline="") as f:
    reader = csv.DictReader(f)
    for row in reader:
        print(row["name"], "->", row["score"])

# -------------------------------------------------------
# Print only students with score > 80
# -------------------------------------------------------
print("\n=== Score > 80 ===")
with open("data/students.csv", newline="") as f:
    reader = csv.DictReader(f)
    for row in reader:
        if int(row["score"]) > 80:
            print(row["name"], row["score"])

# -------------------------------------------------------
# Find the student with the highest score
# -------------------------------------------------------
print("\n=== Highest scorer ===")
with open("data/students.csv", newline="") as f:
    reader = csv.DictReader(f)
    best_name = ""
    best_score = -1
    for row in reader:
        current_score = int(row["score"])
        if current_score > best_score:
            best_score = current_score
            best_name = row["name"]
print("Top student:", best_name, "with score", best_score)

# -------------------------------------------------------
# Experiment 1: print names sorted alphabetically
# -------------------------------------------------------
print("\n=== Alphabetical names ===")
with open("data/students.csv", newline="") as f:
    reader = csv.DictReader(f)
    names = [row["name"] for row in reader]
print(sorted(names))

# -------------------------------------------------------
# Experiment 2: count students per grade
# -------------------------------------------------------
print("\n=== Students per grade ===")
grade_counts = {"A": 0, "B": 0, "C": 0}
with open("data/students.csv", newline="") as f:
    reader = csv.DictReader(f)
    for row in reader:
        grade = row["grade"]
        if grade in grade_counts:
            grade_counts[grade] += 1
for grade, count in grade_counts.items():
    print(f"  Grade {grade}: {count} student(s)")

# -------------------------------------------------------
# Experiment 3: student with the lowest score
# -------------------------------------------------------
print("\n=== Lowest scorer ===")
with open("data/students.csv", newline="") as f:
    reader = csv.DictReader(f)
    worst_name = ""
    worst_score = 9999
    for row in reader:
        current_score = int(row["score"])
        if current_score < worst_score:
            worst_score = current_score
            worst_name = row["name"]
print("Lowest student:", worst_name, "with score", worst_score)
\end{lstlisting}

\begin{hint}
Every value that \texttt{csv.DictReader} gives you is a \textbf{string}, even numbers. The file stores \texttt{"92"} as text, so you must convert: use \texttt{int(row['score'])} for whole numbers or \texttt{float(row['score'])} for decimals before doing any arithmetic or comparisons.
\end{hint}

\begin{warning}
Remember: \texttt{csv.DictReader} gives you strings, not numbers. Always convert before comparing! Writing \texttt{row['score'] > 80} compares a string to an integer-Python will raise a \texttt{TypeError}. Write \texttt{int(row['score']) > 80} instead.
\end{warning}

% -----------------------------------------------------------
\task{Computing Statistics}

Now let's implement our own statistics functions-no importing the \texttt{statistics} module allowed! This is great practice with loops and accumulators.

\textbf{Create \texttt{statistics.py}:}

\begin{lstlisting}
import csv


def column_min(data, col_name):
    """Return the minimum numeric value in a named column.

    Parameters
    ----------
    data : list of dict
        Rows loaded from a CSV file (e.g. via csv.DictReader).
    col_name : str
        The name of the column to inspect.

    Returns
    -------
    float
        The smallest value found in that column.
    """
    # Step 1: Start with the first row's value as the current minimum
    current_min = float(data[0][col_name])
    # Step 2: Loop through the remaining rows
    for row in data[1:]:
        value = float(row[col_name])
        # Step 3: If this value is smaller, update the minimum
        if value < current_min:
            current_min = value
    # Step 4: Return the minimum
    return current_min


def column_max(data, col_name):
    """Return the maximum numeric value in a named column.

    Parameters
    ----------
    data : list of dict
        Rows loaded from a CSV file.
    col_name : str
        The name of the column to inspect.

    Returns
    -------
    float
        The largest value found in that column.
    """
    # Similar to column_min but track the maximum instead
    current_max = float(data[0][col_name])
    for row in data[1:]:
        value = float(row[col_name])
        if value > current_max:
            current_max = value
    return current_max


def column_avg(data, col_name):
    """Return the average (mean) of a numeric column.

    Parameters
    ----------
    data : list of dict
        Rows loaded from a CSV file.
    col_name : str
        The name of the column to average.

    Returns
    -------
    float
        The mean value, or 0.0 if data is empty.
    """
    # Step 1: Initialise running total and a counter
    total = 0.0
    count = 0
    # Step 2: Loop through every row
    for row in data:
        # Step 3: Add the converted value and increment the counter
        total += float(row[col_name])
        count += 1
    # Step 4: Return the average; guard against an empty dataset
    if count == 0:
        return 0.0
    return total / count


# -------------------------------------------------------
# Test code: load the data, call all three functions
# -------------------------------------------------------
with open("data/students.csv", newline="") as f:
    data = list(csv.DictReader(f))

print("=== Score Statistics ===")
print(f"  Min score : {column_min(data, 'score'):.1f}")
print(f"  Max score : {column_max(data, 'score'):.1f}")
print(f"  Avg score : {column_avg(data, 'score'):.2f}")

# -------------------------------------------------------
# Experiment 1: stats for each grade group separately
# -------------------------------------------------------
print("\n=== Stats by grade ===")
for grade in ["A", "B", "C"]:
    group = [row for row in data if row["grade"] == grade]
    if group:
        print(f"  Grade {grade}: "
              f"min={column_min(group, 'score'):.1f}  "
              f"max={column_max(group, 'score'):.1f}  "
              f"avg={column_avg(group, 'score'):.2f}")

# -------------------------------------------------------
# Experiment 2: what happens with a non-numeric column?
# Try: column_min(data, 'name')
# Uncomment the line below and run to see the error.
# -------------------------------------------------------
# print(column_min(data, "name"))
\end{lstlisting}

\begin{hint}
To load an entire CSV file into a list of dictionaries at once, use this pattern:
\begin{lstlisting}[numbers=none]
with open("data/students.csv", newline="") as f:
    data = list(csv.DictReader(f))
\end{lstlisting}
Once the \texttt{with} block ends the file is closed, but \texttt{data} stays in memory as a plain Python list that you can loop over as many times as you like.
\end{hint}

% -----------------------------------------------------------
\task{Filtering Rows}

Filtering means keeping only the rows that satisfy some condition-just like applying a colour filter to an image keeps only the pixels that pass a test.

\textbf{Create \texttt{filter\_rows.py}:}

\begin{lstlisting}
import csv


def filter_rows(data, col_name, value):
    """Return only rows where col_name exactly equals value.

    Parameters
    ----------
    data : list of dict
        The full dataset.
    col_name : str
        Column to test.
    value : str
        The exact string value to match.

    Returns
    -------
    list of dict
        A new list containing only the matching rows.
    """
    # Step 1: Create an empty result list
    result = []
    # Step 2: Loop through each row
    for row in data:
        # Step 3: If the column matches, keep the row
        if row[col_name] == value:
            result.append(row)
    # Step 4: Return the filtered list
    return result


def filter_rows_above(data, col_name, threshold):
    """Return rows where the numeric column value is strictly above threshold.

    Parameters
    ----------
    data : list of dict
        The full dataset.
    col_name : str
        The numeric column to test.
    threshold : float
        Keep rows whose value is greater than this number.

    Returns
    -------
    list of dict
        A new list containing only the matching rows.
    """
    # Similar to filter_rows but convert to float and compare numerically
    result = []
    for row in data:
        if float(row[col_name]) > threshold:
            result.append(row)
    return result


def filter_rows_between(data, col_name, low, high):
    """Return rows where low <= numeric value <= high.

    Parameters
    ----------
    data : list of dict
        The full dataset.
    col_name : str
        The numeric column to test.
    low : float
        Inclusive lower bound.
    high : float
        Inclusive upper bound.

    Returns
    -------
    list of dict
        Rows whose column value falls within [low, high].
    """
    result = []
    for row in data:
        val = float(row[col_name])
        if low <= val <= high:
            result.append(row)
    return result


# -------------------------------------------------------
# Load dataset
# -------------------------------------------------------
with open("data/students.csv", newline="") as f:
    data = list(csv.DictReader(f))

# Filter for Halifax students
print("=== Halifax students ===")
halifax = filter_rows(data, "city", "Halifax")
for row in halifax:
    print(" ", row["name"])

# Filter for score above 80
print("\n=== Score above 80 ===")
high_scores = filter_rows_above(data, "score", 80)
for row in high_scores:
    print(f"  {row['name']}: {row['score']}")

# -------------------------------------------------------
# Experiment 1: students from Truro with score above 70
# -------------------------------------------------------
print("\n=== Truro students with score > 70 ===")
truro = filter_rows(data, "city", "Truro")
truro_above_70 = filter_rows_above(truro, "score", 70)
for row in truro_above_70:
    print(f"  {row['name']}: {row['score']}")

# -------------------------------------------------------
# Experiment 2: students with grade B or C
# -------------------------------------------------------
print("\n=== Grade B or C students ===")
b_or_c = []
for row in data:
    if row["grade"] == "B" or row["grade"] == "C":
        b_or_c.append(row)
for row in b_or_c:
    print(f"  {row['name']}: grade {row['grade']}, score {row['score']}")
\end{lstlisting}

\begin{note}
Filtering rows is the same idea as filtering pixels in an image: you loop over every item and keep only the ones that pass a test. The structure of the function-empty list, loop, conditional append, return-appears constantly in data processing code. Memorise it!
\end{note}

% -----------------------------------------------------------
\task{Writing a CSV File}

Reading data is only half the story. Often you want to save your results so another program (or a human!) can use them.

\textbf{Create \texttt{write\_csv.py}:}

\begin{lstlisting}
import csv

# -------------------------------------------------------
# Load the original data
# -------------------------------------------------------
with open("data/students.csv", newline="") as f:
    reader = csv.DictReader(f)
    fieldnames = reader.fieldnames          # remember the column names
    data = list(reader)

# -------------------------------------------------------
# Filter: keep only students with score >= 80
# -------------------------------------------------------
high_achievers = [row for row in data if int(row["score"]) >= 80]

# -------------------------------------------------------
# Write filtered rows to a new CSV
# -------------------------------------------------------
with open("data/high_achievers.csv", "w", newline="") as f:
    writer = csv.DictWriter(f, fieldnames=fieldnames)
    writer.writeheader()                    # write the column names first
    for row in high_achievers:
        writer.writerow(row)

print("Saved", len(high_achievers), "rows to data/high_achievers.csv")

# -------------------------------------------------------
# Second program: city counts summary CSV
# -------------------------------------------------------

# Count students per city using a dictionary
city_counts = {}
for row in data:
    city = row["city"]
    if city in city_counts:
        city_counts[city] += 1
    else:
        city_counts[city] = 1

# Write the summary to data/city_counts.csv
with open("data/city_counts.csv", "w", newline="") as f:
    writer = csv.DictWriter(f, fieldnames=["city", "count"])
    writer.writeheader()
    for city, count in city_counts.items():
        writer.writerow({"city": city, "count": count})

print("City counts:")
for city, count in city_counts.items():
    print(f"  {city}: {count}")
print("Saved city summary to data/city_counts.csv")

# -------------------------------------------------------
# Experiment 1: write only grade-A students to a CSV
# -------------------------------------------------------
grade_a = [row for row in data if row["grade"] == "A"]
with open("data/grade_a.csv", "w", newline="") as f:
    writer = csv.DictWriter(f, fieldnames=fieldnames)
    writer.writeheader()
    for row in grade_a:
        writer.writerow(row)
print("\nSaved", len(grade_a), "grade-A rows to data/grade_a.csv")

# -------------------------------------------------------
# Experiment 2: add a pass_fail column and save
# -------------------------------------------------------
for row in data:
    row["pass_fail"] = "Pass" if int(row["score"]) >= 60 else "Fail"

extended_fields = list(fieldnames) + ["pass_fail"]
with open("data/students_pf.csv", "w", newline="") as f:
    writer = csv.DictWriter(f, fieldnames=extended_fields)
    writer.writeheader()
    for row in data:
        writer.writerow(row)
print("Saved pass/fail data to data/students_pf.csv")
\end{lstlisting}

\begin{warning}
Always open files for writing with \texttt{newline=""} (an empty string). On Windows, Python's default text mode adds an extra \texttt{\textbackslash r} carriage-return character, which causes every other line in your CSV to be blank. The \texttt{newline=""} argument hands newline handling over to the \texttt{csv} module, where it belongs.
\end{warning}

% -----------------------------------------------------------
\task{A Complete Data Pipeline}

A \textbf{data pipeline} is a script that takes raw data through a series of clearly defined steps: load, process, analyse, save. This is exactly what data scientists do every day.

\textbf{Create \texttt{pipeline.py}:}

\begin{lstlisting}
import csv


# -------------------------------------------------------
# Helper functions (copy from earlier tasks)
# -------------------------------------------------------

def column_avg(data, col_name):
    """Return the average of a numeric column."""
    total = 0.0
    count = 0
    for row in data:
        total += float(row[col_name])
        count += 1
    if count == 0:
        return 0.0
    return total / count


def column_min(data, col_name):
    """Return the minimum of a numeric column."""
    current_min = float(data[0][col_name])
    for row in data[1:]:
        val = float(row[col_name])
        if val < current_min:
            current_min = val
    return current_min


def column_max(data, col_name):
    """Return the maximum of a numeric column."""
    current_max = float(data[0][col_name])
    for row in data[1:]:
        val = float(row[col_name])
        if val > current_max:
            current_max = val
    return current_max


# -------------------------------------------------------
# Step 1: Load data
# -------------------------------------------------------
with open("data/students.csv", newline="") as f:
    reader = csv.DictReader(f)
    fieldnames = reader.fieldnames
    data = list(reader)

print(f"Loaded {len(data)} students from data/students.csv")

# -------------------------------------------------------
# Step 2: Print overall summary
# -------------------------------------------------------
print("\n=== Overall Summary ===")
print(f"  Total students : {len(data)}")
print(f"  Min score      : {column_min(data, 'score'):.1f}")
print(f"  Max score      : {column_max(data, 'score'):.1f}")
print(f"  Avg score      : {column_avg(data, 'score'):.2f}")

# -------------------------------------------------------
# Step 3: Print students above the class average
# -------------------------------------------------------
avg = column_avg(data, "score")
print(f"\n=== Students above class average ({avg:.2f}) ===")
above_avg = []
for row in data:
    if float(row["score"]) > avg:
        above_avg.append(row)
        print(f"  {row['name']}: {row['score']}")

# -------------------------------------------------------
# Step 4: Group students by city
# -------------------------------------------------------
city_scores = {}          # city -> list of scores
for row in data:
    city = row["city"]
    score = float(row["score"])
    if city not in city_scores:
        city_scores[city] = []
    city_scores[city].append(score)

# -------------------------------------------------------
# Step 5: Print average score per city
# -------------------------------------------------------
print("\n=== Average Score by City ===")
for city, scores in city_scores.items():
    city_avg = sum(scores) / len(scores)
    print(f"  {city}: {city_avg:.2f}  (n={len(scores)})")

# -------------------------------------------------------
# Step 6: Save students above average to a new CSV
# -------------------------------------------------------
with open("data/above_average.csv", "w", newline="") as f:
    writer = csv.DictWriter(f, fieldnames=fieldnames)
    writer.writeheader()
    for row in above_avg:
        writer.writerow(row)

print(f"\nSaved {len(above_avg)} above-average students to "
      f"data/above_average.csv")
print("Pipeline complete.")
\end{lstlisting}

\begin{note}
This script is a \textbf{data pipeline}-a sequence of clearly labelled steps that transforms raw data into useful output. Real data science workflows follow exactly this pattern: load, clean, analyse, visualise, save. Once you can write a pipeline for a small dataset like this, scaling up to millions of rows is mostly a matter of using faster tools (like \texttt{pandas}) with the same conceptual structure.
\end{note}


% -----------------------------------------------------------

\section{Challenge Questions}

% -----------------------------------------------------------
\subsection*{Challenge 1: Median Score}

The \textbf{median} is the middle value when all values are sorted. For an even number of values, it is the average of the two middle values.

\textbf{In a new file \texttt{challenge1.py}:}
\begin{itemize}
    \item Load \texttt{data/students.csv} into a list of dicts.
    \item Write a function \texttt{column\_median(data, col\_name)} that computes the median without using any built-in statistics library.
    \item Print the median score. (Expected answer: \textbf{79.5})
    \item Print whether the median is higher or lower than the mean.
\end{itemize}

\begin{hint}
You will need to extract all values into a plain list, sort it with \texttt{sorted()}, then decide whether the length is odd or even using the \texttt{\%} operator.
\end{hint}

% -----------------------------------------------------------
\subsection*{Challenge 2: Score Histogram (Text)}

A \textbf{histogram} groups values into bins and shows how many fall in each bin. You will build one using only \texttt{print()} and string repetition.

\textbf{In a new file \texttt{challenge2.py}:}
\begin{itemize}
    \item Define bins: \texttt{50--59}, \texttt{60--69}, \texttt{70--79}, \texttt{80--89}, \texttt{90--99}.
    \item Count how many students fall in each bin.
    \item Print a text bar chart where each \texttt{*} represents one student, for example:
\end{itemize}

\begin{lstlisting}[language={},numbers=none]
50-59 |
60-69 | **
70-79 | **
80-89 | **
90-99 | **
\end{lstlisting}

\begin{hint}
Use \texttt{"*" * count} to build the bar for each bin. Use integer division (\texttt{//}) and the \texttt{\%} operator, or a chain of \texttt{if}/\texttt{elif} statements, to decide which bin a score belongs to.
\end{hint}

% -----------------------------------------------------------
\subsection*{Challenge 3: Ranking Table}

Sorting rows and adding a computed column are two of the most common data-manipulation tasks.

\textbf{In a new file \texttt{challenge3.py}:}
\begin{itemize}
    \item Load the data and sort all students by score from highest to lowest. Do \textbf{not} use any library sort functions on the raw list; implement a simple selection sort or insertion sort yourself.
    \item Add a \texttt{"rank"} key to each row dict (1 = highest score).
    \item Add a \texttt{"letter\_grade"} key: \texttt{A} for score $\geq 90$, \texttt{B} for $\geq 75$, \texttt{C} for $\geq 60$, \texttt{F} otherwise.
    \item Write the ranked, annotated rows to \texttt{data/rankings.csv} with columns \texttt{rank}, \texttt{name}, \texttt{score}, \texttt{letter\_grade}.
    \item Print the table to the terminal as well.
\end{itemize}

\begin{note}
A \textbf{selection sort} finds the largest remaining value on each pass and swaps it to the front of the unsorted portion. You already know all the tools you need: a loop, an accumulator for the current best, and list indexing.
\end{note}

% -----------------------------------------------------------
\subsection*{Challenge 4: Multi-File Summary}

Real datasets are often split across several files. Here you will create a second data file and write code that processes both at once.

\textbf{Step 1: create \texttt{data/teachers.csv}:}
\begin{lstlisting}[language={},numbers=none,frame=single]
name,subject,years_experience,city
Ms. Park,Math,8,Halifax
Mr. Reid,Science,3,Sydney
Ms. Chen,English,12,Halifax
Mr. Osei,History,5,Truro
\end{lstlisting}

\textbf{Step 2: in a new file \texttt{challenge4.py}:}
\begin{itemize}
    \item Write a \textbf{generic} function \texttt{load\_csv(filepath)} that works for \emph{any} CSV file and returns a list of dicts.
    \item Load both \texttt{students.csv} and \texttt{teachers.csv} using that function.
    \item Print every unique city that appears in \textbf{either} file (no duplicates, sorted alphabetically).
    \item For each city, print how many students \emph{and} how many teachers are based there.
    \item Save a summary to \texttt{data/city\_summary.csv} with columns \texttt{city}, \texttt{student\_count}, \texttt{teacher\_count}.
\end{itemize}

\begin{hint}
Build a set of cities first (sets automatically remove duplicates), then loop over the sorted set to count occurrences in each list separately.
\end{hint}

% -----------------------------------------------------------
\subsection*{Challenge 5: Data Cleaning}

CSV files from the real world are often messy. This challenge practices \textbf{defensive programming}: detecting and correcting bad data before processing it.

\textbf{Step 1: create \texttt{data/students\_dirty.csv}:}
\begin{lstlisting}[language={},numbers=none,frame=single]
name,grade,score,city
Alice,A,92,Halifax
,B,78,Truro
Carol,A,105,Halifax
David,C,-5,Sydney
Emma,b,81,Truro
Frank,A,,Halifax
Grace,C,62,Sydney
Henry,B,75,
\end{lstlisting}

The file contains several problems: a missing name, scores outside the valid range 0--100, a lowercase grade, a missing score, and a missing city.

\textbf{Step 2: in a new file \texttt{challenge5.py}:}
\begin{itemize}
    \item Write a function \texttt{clean\_row(row)} that takes a single dict and returns a \textbf{cleaned} copy, applying these rules:
    \begin{itemize}
        \item If \texttt{name} is empty, skip the row (return \texttt{None}).
        \item Convert \texttt{grade} to uppercase.
        \item If \texttt{score} is empty or not a valid integer, set it to \texttt{0}.
        \item Clamp \texttt{score} to the range \texttt{[0, 100]}: values below 0 become 0, values above 100 become 100.
        \item If \texttt{city} is empty, set it to \texttt{"Unknown"}.
    \end{itemize}
    \item Apply \texttt{clean\_row} to every row in \texttt{students\_dirty.csv}, discarding rows where it returns \texttt{None}.
    \item Print both the original and cleaned versions of each row so you can verify the fixes.
    \item Save the cleaned data to \texttt{data/students\_clean.csv}.
\end{itemize}

\begin{hint}
To safely convert a string to an integer when the string might be empty or invalid, use a \texttt{try}/\texttt{except} block:
\begin{lstlisting}[numbers=none]
try:
    score = int(raw_score)
except ValueError:
    score = 0
\end{lstlisting}
\end{hint}

% -----------------------------------------------------------

\section{Checkoff}

Before you leave, show your instructor or TA:
\begin{enumerate}
    \item \texttt{explore\_csv.py} running and printing all rows, the header, and the Halifax count correctly.
    \item \texttt{dict\_reader.py} correctly identifying the highest-scoring student (Carol, 95).
    \item \texttt{statistics.py} printing the correct min (62), max (95), and average score for the full dataset.
    \item \texttt{filter\_rows.py} filtering Halifax students and listing students with score above 80.
    \item \texttt{pipeline.py} running end-to-end without errors and producing \texttt{data/above\_average.csv} with the correct rows.
    \item \textbf{(Bonus)} Any completed challenge files, with correct output visible in the terminal.
\end{enumerate}

% -----------------------------------------------------------

\section{Submission}

Upload the following files to Moodle under \textbf{Lab 07}:

\begin{itemize}
    \item \textbf{(Challenge 1)} \texttt{challenge1.py}
    \item \textbf{(Challenge 2)} \texttt{challenge2.py}
    \item \textbf{(Challenge 3)} \texttt{challenge3.py} and \texttt{data/rankings.csv}
    \item \textbf{(Challenge 4)} \texttt{challenge4.py}, \texttt{data/teachers.csv}, and \texttt{data/city\_summary.csv}
    \item \textbf{(Challenge 5)} \texttt{challenge5.py}, \texttt{data/students\_dirty.csv}, and \texttt{data/students\_clean.csv}
\end{itemize}

% -----------------------------------------------------------


\end{document}
